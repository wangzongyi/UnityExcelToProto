/**
* @file:   Person.proto
* @说明:  这个文件是通过工具自动生成的，建议不要手动修改
*/

syntax = "proto3";

package proto;

message Person{
    /** 唯一标识 */
    string id = 1;
    /** 姓名 */
    string username = 2;
}

message PersonMap {
    map<string, Person> items = 1;
}
